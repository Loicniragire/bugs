The models in this project were generated through EF database-first process. 

- Therefore, we do not make use of the 'EntityConfigurations' as that approach is assuming a Model First EF.
- Command: dotnet ef dbcontext scaffold "Server=localhost:3306;database=SoftwareAnalysis;user=root;password=password"
  MySql.Data.EntityFrameworkCore -o Models
 

